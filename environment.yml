name: strong-drpo
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - --index-url=https://mirrors2.westlake.edu.cn/pypi/simple
      - -e ".[dev]"
      - accelerate>=0.24,<2
      - diffusers>=0.20,<0.38
      - open-clip-torch>=2.20,<4
      - peft>=0.8,<0.20
      - tensorboard>=2.14,<3
      - transformers>=4.35,<6
      - xformers==0.0.31.post1
